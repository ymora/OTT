# ================================================================================
# Configuration Render - OTT API Backend
# ================================================================================
# HAPPLYZ MEDICAL SAS
# Configuration pour déploiement sur Render.com
# ================================================================================
#
# ⚠️ IMPORTANT : Configuration du Persistent Disk
# Pour éviter de retélécharger le core ESP32 (~430MB) à chaque déploiement,
# configurez un Persistent Disk dans le dashboard Render :
# 1. Allez dans votre service ott-api > Disks
# 2. Cliquez sur "Add Disk"
# 3. Mount Path: /opt/render/project/src/arduino-data
# 4. Size: 1 GB (minimum recommandé)
# 5. Enregistrez et déployez
#
# 📖 Documentation complète : docs/RENDER_PERSISTENT_DISK.md
# ================================================================================

services:
  - type: web
    name: ott-api
    env: php
    buildCommand: |
      # ⚠️ CRITIQUE: arduino-cli est OBLIGATOIRE - la compilation ne doit JAMAIS être simulée
      echo "🔧 Installation d'arduino-cli (OBLIGATOIRE)..."
      export PATH="${HOME}/.local/bin:${PATH}"
      bash scripts/hardware/install_arduino_cli.sh
      # S'assurer que le PATH inclut ~/.local/bin pour cette session
      export PATH="${HOME}/.local/bin:${PATH}"
      # Vérifier que arduino-cli est disponible et fonctionnel
      if ! command -v arduino-cli &> /dev/null; then
        echo "❌ ERREUR CRITIQUE: arduino-cli n'est pas disponible après installation"
        echo "❌ Le build est interrompu - la compilation ne peut pas être simulée"
        exit 1
      fi
      # Vérifier que arduino-cli fonctionne
      if ! arduino-cli version &> /dev/null; then
        echo "❌ ERREUR CRITIQUE: arduino-cli est installé mais ne fonctionne pas"
        exit 1
      fi
      echo "✅ arduino-cli disponible et fonctionnel: $(arduino-cli version)"
      # Vérifier si le core ESP32 est déjà dans hardware/arduino-data/ (versionné avec GitHub LFS)
      if [ -d "hardware/arduino-data/packages/esp32/hardware/esp32" ]; then
        echo "✅ Core ESP32 trouvé dans hardware/arduino-data/ (versionné avec le projet)"
        echo "✅ Pas besoin d'installation - le core est déjà dans le repo"
      else
        echo "⚠️ Core ESP32 non trouvé dans hardware/arduino-data/"
        echo "⚠️ Le core sera téléchargé lors de la première compilation"
        echo "⚠️ Pour éviter cela, installez le core localement et ajoutez-le à GitHub LFS"
      fi
      echo "✅ La compilation sera RÉELLE, jamais simulée"
    startCommand: |
      export PATH="${HOME}/.local/bin:${PATH}"
      php -S 0.0.0.0:$PORT -t .
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_EMAIL
        sync: false
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_FROM_NUMBER
        sync: false

