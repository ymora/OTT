name: Deploy Next.js to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Generate SUIVI_TEMPS_FACTURATION.md
        run: |
          echo "📄 Génération du fichier SUIVI_TEMPS_FACTURATION.md..."
          # Créer un fichier minimal si le script PowerShell n'est pas disponible
          # Le fichier sera régénéré via l'API si nécessaire
          cat > public/SUIVI_TEMPS_FACTURATION.md << 'EOF'
          # Suivi du Temps - Projet OTT
          ## Journal de travail pour facturation (Généré automatiquement)
          
          > **Note**: Ce fichier est généré automatiquement. Pour une version complète, utilisez le script d'audit ou l'API.
          
          **Période analysée** : En cours
          **Développeur** : ymora
          
          ### Statistiques
          - **Total heures** : En cours de calcul...
          - **Total commits** : En cours de calcul...
          
          ---
          *Ce fichier sera mis à jour lors du prochain audit complet.*
          EOF
          echo "✅ Fichier créé dans public/"
          ls -lh public/SUIVI_TEMPS_FACTURATION.md || echo "⚠️  Fichier non trouvé après création"
      
      - name: Build & export static site
        run: bash scripts/deploy/export_static.sh
        env:
          NEXT_PUBLIC_API_URL: https://ott-jbln.onrender.com
          NEXT_PUBLIC_ENABLE_DEMO_RESET: 'false'
          NEXT_STATIC_EXPORT: 'true'
          NEXT_PUBLIC_BASE_PATH: '/OTT'
          NODE_ENV: 'production'
      
      - name: Verify build output
        run: |
          echo "Contenu du dossier out/:"
          ls -la out/ | head -20
          # Utiliser le script de vérification dédié
          bash scripts/deploy/verify-build.sh out
      
      - name: Ensure .nojekyll exists
        run: touch out/.nojekyll
      
      - name: Clean output directory
        run: |
          # Supprimer README.md s'il existe dans out/ (pour éviter que GitHub Pages le serve)
          if [ -f "out/README.md" ]; then
            rm out/README.md
          fi
          
          # PROTECTION: Vérifier qu'il n'y a pas de fichiers HTML à la racine sauf index.html et 404.html
          echo "🔍 Vérification des fichiers HTML à la racine..."
          html_files=$(find out -maxdepth 1 -name "*.html" -type f ! -name "index.html" ! -name "404.html")
          if [ -n "$html_files" ]; then
            echo "❌ ERREUR: Fichiers HTML trouvés à la racine de out/ (sauf index.html et 404.html):"
            echo "$html_files"
            echo "⚠️  Ces fichiers peuvent être servis par GitHub Pages au lieu de index.html"
            echo "📝 Solution: Déplacer ces fichiers dans un sous-dossier (ex: docs/)"
            exit 1
          else
            echo "✅ Aucun fichier HTML indésirable à la racine"
          fi
          
          # Vérifier que index.html existe et contient l'application
          if [ -f "out/index.html" ]; then
            echo "✅ index.html trouvé"
            # Vérifier que index.html contient bien l'application React
            if ! grep -q "OTT Dashboard\|root\|__next" out/index.html; then
              echo "⚠️  index.html ne semble pas contenir l'application React"
              echo "⚠️  Vérifiez que le build Next.js s'est bien déroulé"
            else
              echo "✅ index.html contient l'application React"
            fi
          else
            echo "❌ ERREUR: index.html manquant"
            exit 1
          fi
          
          # Vérifier que les fichiers de documentation sont bien dans docs/
          if [ -d "out/docs" ]; then
            doc_count=$(find out/docs -name "DOCUMENTATION_*.html" -type f | wc -l)
            if [ "$doc_count" -gt 0 ]; then
              echo "✅ $doc_count fichier(s) de documentation trouvé(s) dans out/docs/"
            fi
          fi
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out
  
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

