name: Notify Admin on Push

on:
  push:
    branches: [ main ]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send email notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "🚀 OTT - Nouveau push sur main"
          to: maxime@happlyzmedical.com,ymora@free.fr
          from: "OTT System <noreply@happlyz.com>"
          body: |
            Bonjour,

            Un nouveau push a été effectué sur le repository OTT :

            📦 Commit: ${{ github.sha }}
            📝 Message: ${{ github.event.head_commit.message }}
            👤 Auteur: ${{ github.event.head_commit.author.name }}
            📅 Date: ${{ github.event.head_commit.timestamp }}

            🔗 Voir sur GitHub: https://github.com/ymora/OTT/commit/${{ github.sha }}

            ---
            Ceci est une notification automatique du système OTT
            HAPPLYZ MEDICAL SAS
